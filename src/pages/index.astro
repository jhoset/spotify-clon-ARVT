---
import { playlists } from "@/lib/data";
import Layout from "../layouts/Layout.astro";
import PlayListItemCard from "@/components/PlayListItemCard.astro";
import RecentPlayItemCard from "@/components/RecentPlayItemCard.astro";
import Greeting from "../components/Greeting.svelte";
import PageHeader from "../components/PageHeader.astro";
---

<Layout title="Spotify Clone :D">
    <div
        id="playlist-container"
        class="relative transition-all duration-1000 bg-cyan-950/70"
    >
        <!--  <PageHeader />  -->
        <PageHeader />

        <div class="relative z-10 px-6">
            <Greeting />
            <div
                class="grid gap-y-4 gap-x-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 mt-6"
            >
                {
                    playlists.map((playlist) => (
                        <RecentPlayItemCard playList={playlist} />
                    ))
                }
            </div>
            <h1 class="text-3xl font-bold pt-10">Made for you</h1>
            <div class="flex flex-wrap mt-6 gap-4">
                {
                    playlists.map((playlist) => (
                        <PlayListItemCard playList={playlist} />
                    ))
                }
            </div>
        </div>

        <div
            class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/95 -z-1"
        >
        </div>
    </div>
</Layout>

<style></style>
